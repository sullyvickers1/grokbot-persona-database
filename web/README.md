# Catalog

Open `index.html` in a browser. No build, no install.

This folder is generated for reading and copying prompts. YAML in `../personas/` is the source of truth. After you change a persona, run `npm run index` from the repo root so `personas.json` stays current.
