# The public site is the gh-pages branch (static files at the root).
# Do not deploy from main. Users open GitHub Pages, they do not run this repo.
name: Catalog pages

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Site is published from the gh-pages branch. Do not deploy from main."
